# 功能说明文档存放处（示例占位）

> 实际项目的功能说明文档请放在**项目根目录**的 `docs/features/` 下，不要放在 `ai-dev-guide/` 里——本套件目录只放规则和模板，不存放实际项目文档。

实际使用时：

1. 在项目根目录创建 `docs/features/`；
2. 每个功能按 `part2-开发/templates/T8-功能说明文档模板.md` 生成 `F-<编号>-<功能名>.md`；
3. 编号与 `docs/requirements/T2-功能清单.md` 一一对应。
